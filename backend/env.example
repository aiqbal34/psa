PORT=5000
NODE_ENV=development

# PostgreSQL Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/ucdpakipsa_polls
# Set DB_SSL=true if your provider requires SSL on TCP connections
# DB_SSL=false

# Alternative discrete configuration (used locally or with Cloud SQL sockets)
# DB_USER=ucdpakipsa_user
# DB_PASSWORD=supersecret
# DB_NAME=ucdpakipsa_polls
# DB_HOST=127.0.0.1
# DB_PORT=5432

# For Google Cloud SQL (Postgres) via Unix socket on Cloud Run
# CLOUD_SQL_CONNECTION_NAME=project:region:instance
# When CLOUD_SQL_CONNECTION_NAME is set, the app will connect via /cloudsql socket

# For Render deployment, use the External Database URL provided by Render:
# DATABASE_URL=postgresql://username:password@hostname:5432/database_name
# Example External URL: postgresql://aiqbal34:password@dpg-d29etg7diees73clqh9g-a.oregon-postgres.render.com/ucdpakipsa_polls

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,https://ucdpakipsa.io,https://ucdpakipsa.vercel.app,https://psa-oboc.vercel.app